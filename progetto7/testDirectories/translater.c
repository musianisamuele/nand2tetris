#include "global.h"

void multi_file_translater (plistas main_code, plistaf functions) {
	/* In questa funzione ho già la lista di comandi del main. Ho già la lista di funzioni
	 * con relativo codice. Devo inziare la vera e propria traduzione: creo quindi una lista
	 * di stringhe che conterrà le funzioni che devono essere tradotte. Inserirò Sys.init
	 * e il codice del Main. Poi appena incontro una chiamata a funzione che non ho già tradotto
	 * allora la inserirà nella lista in coda e continuerò la mia traduzione.
	*/
}
